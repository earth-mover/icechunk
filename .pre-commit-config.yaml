repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
        exclude: 'icechunk-js/\.yarn/releases/.*'
      - id: end-of-file-fixer
        exclude: 'icechunk-js/\.yarn/releases/.*'
      - id: check-yaml
        exclude: docs/mkdocs.yml
      - id: debug-statements
      - id: mixed-line-ending

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: 'v0.15.3'
    hooks:
      - id: ruff-format
      - id: ruff
        args: ["--fix", "--show-fixes", "icechunk-python/"]

  - repo: https://github.com/rhysd/actionlint
    rev: v1.7.11
    hooks:
      - id: actionlint
        files: ".github/workflows/"
        args: ["-ignore", "SC1090", "-ignore", "SC2046", "-ignore", "SC2086", "-ignore", "SC2129", "-ignore", "SC2155"]

  - repo: https://github.com/codespell-project/codespell
    rev: v2.3.0
    hooks:
      - id: codespell

  - repo: https://github.com/zizmorcore/zizmor-pre-commit
    rev: v1.24.1
    hooks:
    - id: zizmor
      files: \.github\/workflows\/(code-quality|dependency-check|python-upstream|publish-rust-library|rust-ci|rust-upstream|windows-check)\.ya?ml
      args:
        - "--no-progress" # https://github.com/zizmorcore/zizmor/issues/582
        - "--pedantic"

  - repo: local
    hooks:
      - id: rust-pre-commit-fast
        name: "Fast Rust checks (format, lint)"
        entry: just pre-commit-fast
        language: system
        files: \.(rs|toml)$|Cargo\.lock$|justfile$
        pass_filenames: false

      - id: rust-pre-commit
        name: "Medium Rust checks (compilation, deps)"
        entry: just pre-commit
        language: system
        files: \.(rs|toml)$|Cargo\.lock$|justfile$
        pass_filenames: false
        stages: [pre-push]

      - id: rust-pre-commit-ci
        name: "Full CI checks (tests, examples)"
        entry: just pre-commit-ci
        language: system
        files: \.(rs|toml)$|Cargo\.lock$|justfile$
        pass_filenames: false
        stages: [manual]

exclude: 'tests/data/.*'
