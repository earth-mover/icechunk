# Python API Reference

## Top-level

The core entry point. Only `Repository` and a few utilities live here — everything else is in submodules.

::: icechunk
    options:
      members: [Repository, print_debug_info, upgrade_icechunk_repository, supported_spec_versions]
